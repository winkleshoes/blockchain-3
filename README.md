# blockchain semarang pertemuan 3

sebelum di test install node modules:
npm install --save solc

untuk windows install juga:
npm install --global --production windows-build-tools (run administrator)
